# TODO

这是本项目的**工作清单**：接下来要做什么、为什么、怎么验证做完。公开文档以英文为主，这份内部计划保持中文，和 `AGENTS.md` 一致。

**规则**：改动检索相关的任何东西（策略、打分、配置项），必须先有回放证据，见 [docs/SELF_EVOLUTION_CN.md](docs/SELF_EVOLUTION_CN.md)。没有证据的"我觉得这样更好"不进这个清单。

优先级按"单位工作量的价值"排，P0 是下一件该做的事。条目编号一经分配就不再改（引用过的地方保持有效），所以编号不连续是正常的。

「已完成」里的每一条都要带可复现的验证方式：命令、测试名或测量数字。写不出验证方式的改动不算完成，算"改过"。

---

## P0 · 下一步（工程欠账）

### 4. 摄取改成后台任务：队列与进度

- **为什么**：摄取仍然同步阻塞在 HTTP 请求里（去重已完成，见「已完成」）。大文件与慢 embedding 会让请求挂住。
- **做什么**：摄取进队列 + 进度查询；模型相关的主题维护本就在事务外，适合并入队列。
- **怎么验证**：大文件摄取的请求能立即返回并在状态接口看到进度；失败可以从状态接口读到原因。

### 5. 更新路径的余下部分

- **现状**：`PUT /api/knowledge-bases/{id}/documents/{document_id}` 已交付（见「已完成」），README 有 curl 用法，MCP 有 `update_source_in_wiki`。仍未覆盖：
  - **重传文件**没有出口：`PUT` 可以改文件来源的正文（`file_name` 保留原值），但不能换一个 `.txt` 重新摄取，也没有 `PUT .../file`。
  - **主题页不搬迁**：更新只沿用原主题页，即使标题改动后按确定性归属规则本该落到别处。要搬迁就得先定规则（改标题触发？新页面继承什么？）并同步改写指向它的 `[[链接]]`，否则宁可不动。
  - 每次更新都 `rebuild_index`，大工作区的代价还没量过。
- **怎么验证**：文件来源可原地更新且路径不变；若实现搬迁，则搬迁后不产生断链、`index.md` 与 `sources:` 列表同步。

### 6. 内容级 lint

- **为什么**：现在的 lint 只查结构（断链 / 孤立页 / 缺摘要），不查**内容**：页面之间的矛盾、被更新来源取代的结论、被提到却没有独立页面的概念。
- **做什么**：按 [docs/SELF_EVOLUTION_CN.md](docs/SELF_EVOLUTION_CN.md) 的"知识轴"来设计——lint + 图谱缺口生成一份**缺口清单**，再由外部 Agent 去研究、摄取、复验。先只做"缺口清单"这一步。
- **怎么验证**：用现有知识库跑一次，人工判断清单里是否有真实缺口；不产生任何自动改写。

### 7. 回放覆盖原始资料路径

- **为什么**：`replay_queries.py` 目前只回放 `mode="wiki"` 的反馈；RAG 那条路的引用格式是 `document:<id>#<chunk>`，需要向量库。
- **做什么**：让回放能重放 `mode="rag"` 的记录，比较分块级保留。
- **怎么验证**：对已有 RAG 评分跑一次，输出与 wiki 模式同构的对比表。

---

## P1 · 产品与体验

### 8. 模型回答的流式输出

- **为什么**：`POST /api/search/stream` 目前只发检索事件（`start/delta/result/error/done`）。把模型 token 塞进旧契约会让老客户端误解。
- **做什么**：**先设计并记录新的事件契约**，再实现；旧 SSE 路径不得吞掉模型回答。
- **怎么验证**：契约写成文档并有测试；老客户端只读检索事件时行为不变。

### 9. `related` 页面 → 继续追问

- **为什么**：工作台的角色是"把多步探索的工具给人"。顺着一条关联页继续问是最自然的一步。
- **做什么**：给查询加一个可选的 `scope`（页面路径），把候选限定在该页及其邻居；界面上在引用/关联条目上提供"在这一页里继续问"。
- **怎么验证**：限定 scope 时结果只来自该页邻域；不带 scope 时行为与现在一致。

### 10. `purpose.md` 参与候选集

- **为什么**：`purpose.md` 现在只作为"工作区意图"注入提示词，不参与页面排序（查询只读 `wiki/` 下的页面）。
- **做什么**：决定它该作为可引用结果还是永远只作上下文。若作为结果，需要明确它的 `page_type` 与排序权重。
- **怎么验证**：改与不改各跑一次回放，看 👍 用例的引用保留是否受影响。

### 11. MCP 暴露回放

- **为什么**：外部 Agent 现在必须 shell 出去跑 `replay_queries.py` 才能做对比。
- **做什么**：把回放结果作为只读工具暴露（例如 `replay_strategy_comparison`），或在 MCP 里提供"读取提案"的能力。
- **注意**：回放要读 SQLite 与工作区，属于 Backend 的活；不要在 MCP 里重实现。
- **怎么验证**：同一份反馈数据上，MCP 工具返回的对比结果与 `replay_queries.py` 输出一致（字段与数值都对得上）。

### 14. 大工作区的图谱只该画枢纽

- **实测**（生产构建，800 页 / 1600 边）：打开图谱 315ms，其中**布局 151ms**、React + SVG 渲染约 150ms；主线程阻塞 30ms 量级。25 页是 100ms。
- **为什么还要做**：800 页的图谱在视觉上是一团毛球，看不出结构——即使它现在打开得足够快。`GraphSheet` 的标签避让已经把打印标签限制在 10 个，但节点和边仍然全画。
- **做什么**：超过阈值（例如 300 页）只画**度数最高的枢纽及其邻居**，并如实说明"还有 N 页未显示"，而不是静默截断。这是显示层的取舍，不改变图谱作为派生数据的性质。
- **怎么验证**：800 页图谱仍能一眼看出结构，且不出现"看起来只有这些页"的误导；阈值以下的视图与现在完全一致。

### 15. 工作台里更新一份来源

- **为什么**：原地更新已能从 API 与 MCP 触发（见「已完成」），但浏览器里改一份来源仍然只能删了重摄——普通用户路径正是最该省掉这一步的地方。
- **做什么**：在来源页（`wiki/sources/*.md`）的阅读视图里提供"重新摄取这段正文"，走 `PUT /api/knowledge-bases/{id}/documents/{document_id}`；需要把 document id 暴露到页面元数据，或按来源页路径反查。要注意区分两件事：编辑 Markdown 页面（保存到 `wiki/`）与重新摄取正文（更新 raw 快照、document 记录与向量），界面上必须说清当前做的是哪一件。
- **怎么验证**：从界面更新后来源页路径不变、主题页来源行不重复、RAG 命中新内容；文件路径（编辑页面）行为不变。

---

## P2 · 交付与运维

### 12. CI 依赖安装太慢

- **现状**：`.github/workflows/ci.yml` 在每次 push 都 `pip install -r requirements.txt`，包含 `chromadb` 与 `sentence-transformers`（体积大、首次安装慢）。
- **做什么**：把测试拆成"不依赖向量栈"与"依赖向量栈"两组；前者用轻量 requirements，保证 CI 快速可靠。
- **怎么验证**：轻量那组在干净环境里 60 秒内跑完；连续三次 push 的 Actions 都不超时。

### 13. 截图与文档会漂移

- **现状**：`demo/` 的四张截图是某个时间点的 UI；文档里的测量数字也是某个 25 页工作区的。图谱布局的基准脚本（`npm run bench:layout`）已随仓库走，但驱动它的浏览器脚本（Playwright）仍在仓库外。
- **做什么**：记录截图与数字的生成方式（脚本 + 数据规模），或在文档里标注测量日期与数据规模。目前 `RETRIEVAL.md` 开头已声明数字是标定值，但没写测量日期。
- **怎么验证**：照文档说明重跑一次能得到同样的截图与同量级数字；文中每个引用到的数字都能指到它的来源（命令或文件）。

---

## 明确不做

| 不做的事 | 原因 |
| --- | --- |
| 后端内置 Agent（工具循环） | 与调用方 Agent 职责重复，违反 `AGENTS.md` 的 MCP 契约，并把成本/漂移/不可复现推进最难回滚的一层 |
| 自动晋升策略 | 即使小改动也应有人确认；`--check` 只做回归护栏，不做自动变更 |
| 评审队列服务 | 这个规模下 Markdown 提案 + git 评审足够；多人并行分诊时才需要 |
| 把点击行为当信号 | 打开一个引用不等于认可，只有显式 👍/👎 才是信号 |
| 用模型改写检索排序 | 模型只能提供检索词（`planned`），排序必须保持确定性 |
| 学习式排序 / rerank 优先做 | BM25 + 向量 + 标题覆盖度已覆盖当前规模；没有具体失败案例前不加 |
| 把正文相同的两个来源合并成一份 | 同一篇文章被两个平台转载是**两份来源**；按内容去重会丢掉 URL、平台、作者与抓取时间，而这些正是溯源要回答的问题。去重只针对"同一来源的重复采集" |

---

## 已完成（近期）

按主题归并，组内新的在前。每条都附可复现的验证方式。

### 摄取与工作区

- **同一来源重复摄取会被拒，而不是产生第二条记录**（原 P0-4 的去重部分）：三个 `POST` 摄取入口在「相同内容 + 相同来源身份」时返回 `409`，消息给出已有 document id 与应改用的 `PUT` 接口。来源身份参与判定：同 URL（或同文件名 / 同标题）才算重摄。`documents` 增加 `tags` 列，此前标签只写进 wiki、无法在更新时保留。
  - 验证：`tests/test_document_update.py`（重复摄取 409、同文不同来源仍是两份来源、文件重传 409）。
- **文档原地更新路径**（原 P0-5）：`PUT /api/knowledge-bases/{id}/documents/{document_id}` 替换正文并可刷新来源元数据，保留 document id、raw 快照路径、来源页路径与主题页来源行；未提交的字段保持原值。单事务内完成，新向量先写、旧向量后删。MCP 补 `update_source_in_wiki`，让 Agent 在收到 409 时有路可走。
  - 顺带修掉三个真实缺陷：(1) SQLite 复用行号会让新 chunk 与旧 chunk 的向量 id 相同，"先加后删"于是把刚写入的新向量删掉——向量 id 因此带上内容哈希；(2) 更新后新 chunk 行的 `vector_id` 为 NULL，会让下一次更新找不到待删向量；(3) 单来源主题页的摘要与 `## Sources` 会自相矛盾——更新时一并刷新该来源的摘要，多来源页面则保留原有正文。另外主题页标题被"焊"在来源行上的排版问题也修掉了。
  - 验证：`tests/test_document_update.py`（11 条，含向量替换顺序与策略改为 `link_only` 时丢弃向量）；真实 Chroma 上同一文档经历 3 个版本后向量库只剩 1 条当前内容。

### 工作台

- **图谱布局不再随工作区规模崩塌**（原 P0-3）：斥力从精确 O(n²) 改为 Barnes-Hut 桶式四叉树近似（`THETA=1.0`、每叶 4 点、叶内仍精确），每条边端点的 `placed.find` 换成 Map。生产构建实测：25 页 116→100ms、400 页 322→192ms、**800 页 956→315ms**。质量对比精确版：边长中位差 1–3%，重叠对与占用面积同级。
  - 验证：`npm run bench:layout`（仓库内脚本，输出耗时与质量指标）；800 页端到端耗时另由仓库外的 Playwright 脚本测量，见条目 13。
- **评分可指认具体哪条引用不对**（旧 P0-1）：`query_feedback.bad_paths`（对旧库做附加式迁移），`POST .../feedback` 拒绝 👍 携带该字段，工作台在 👎 时提供引用勾选。被指认的引用被排除出"该保留"集合，否则任何策略一隐藏它就同时被记成一次回归；提案脚本的改进判据也从"结果变了"换成"隐藏了默认仍返回的被指认引用"。
  - 验证：`tests/test_feedback.py`（字段校验、去重、👍 被拒）、`tests/test_wiki.py`（隐藏被指认引用才算改进、回放如实报告被指认引用）。
- **工作台的「反馈」纸**（旧 P0-2）：第四张纸，只读；读回 `GET .../feedback`，显示 👍/👎 汇总与时间倒序列表（问题、原因、模式、策略、时间、被指认的引用），点问题可填回查询框重跑。不新增任何写路径。
  - 验证：手动点评分后可见；点击问题不改写任何文件。
- **「阅览室」视觉与信息结构重做**：纸/墨色板与三色溯源（自持知识 / 外部来源 / 待处理）、顶部单栏 KPI、边注与页码、引用脚注、四张纸（页面 / 图谱 / 检查 / 反馈）。
  - 验证：`demo/` 四张截图；浏览器冒烟脚本无 console 报错。

### 检索

- 打分重做：CJK 二元组分词、BM25（词频饱和 + 长度归一化）、标题覆盖度、自适应跳数（弱命中才走第二跳）。
- 命名检索策略 `auto` / `local` / `deep` / `hybrid` / `planned`，可从 API、工作台、MCP 选择；响应如实回报 `strategy/hops/vectors/planned`。
- 页面向量召回（带相似度下限 `WIKI_QUERY_VECTOR_FLOOR=0.35`，embedding 不可用时确定性降级）。
- 回答反馈通道（👍/👎 + 原因 + `strategy_id`，永不写入 wiki）。
- 回放工具与提案脚本（`replay_queries.py`、`propose_strategy_change.py`，`--check` 可作回归护栏）。
  - 验证：`propose_strategy_change.py --check` 在默认策略上退出 0，并打印"保留了全部被点赞的引用"。

### 文档

- `docs/` 全部文档中英双版本，并做过数字 / 标识符 / 结构一致性核对。
- `skills/agentkb-retrieval` 把闭环写成外部 Agent 可执行的流程。
- `README` / `README_CN` 与本文同步：能力清单、MCP 工具、来源更新用法、路线图头条。
  - 验证：README 中每个引用到的截图与命令都存在；`npm run build`、两个 pytest 套件通过。
